# Homebrew Tap for Nudge

このリポジトリは Nudge の Homebrew Cask を提供します。

## 使い方

```sh
brew tap yoshihiko555/nudge
brew install nudge
```

## リリース更新

1. GitHub Releases に DMG をアップロード
2. `Casks/nudge.rb` の `version` と `sha256` を更新

